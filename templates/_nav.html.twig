<nav class="nav">
    <a href="{{ path('homepage', { id : app.user.id }) }}" class="nav__link">
        <i class="fas fa-2x fa-smoking"></i>

    <a href="{{ path('stats_user', { id : app.user.id }) }}" class="nav__link">
       <div class="d-flex justify-content-around">
           <i class="fas fa-2x fa-chart-line"></i>
       </div>

    </a>
    <a href="{{ path('user_profile_show', { id : app.user.id }) }}" class="nav__link">
        <i class="fas fa-2x fa-user-astronaut"></i>
    </a>
    {%  if is_granted('ROLE_ADMIN') %}
    <a href="{{ path('boardtable') }}" class="nav__link">
        <i class="fab fa-2x fa-connectdevelop"></i>
    </a>
    {% endif %}
</nav>