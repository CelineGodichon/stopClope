{% extends 'base.html.twig' %}

{% block title %}Cigarette{% endblock %}

{% block body %}
    <h1>Cigarette</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cigarette.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ cigarette.price }}</td>
            </tr>
            <tr>
                <th>IsSmoked</th>
                <td>{{ cigarette.isSmoked ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cigarette_index') }}">back to list</a>

    <a href="{{ path('cigarette_edit', {'id': cigarette.id}) }}">edit</a>

    {{ include('cigarette/_delete_form.html.twig') }}
{% endblock %}
